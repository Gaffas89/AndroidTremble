<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/light_bgd</item>
        <item name="colorPrimaryDark">@color/dark_bgd</item>
        <item name="colorAccent">@color/bgd</item>
        <item name="android:textColorPrimary">@color/dark_bgd</item>
    </style>

</resources>
